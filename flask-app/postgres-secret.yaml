apiVersion: v1
kind: Secret

metadata:
  name: postgres-secret
  namespace: devops-app

type: Opaque

stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres123
  POSTGRES_DB: devopsdb
  DATABASE_URL: postgresql://postgres:postgres123@postgres-service:5432/devopsdb
